export enum DataStateEnum {
  LOADING="Loading",
  SUCCESS="Success",
  ERROR="Error",
  INITIAL="Initial",
}

export enum OperationEnum {
  CREATE = "CREATE",
  UPDATE = "UPDATE",
  ADD = "ADD",
  ERROR = "ERROR"
}