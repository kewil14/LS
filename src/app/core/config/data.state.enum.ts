export enum DataStateEnum {
  LOADING="Loading",
  LOADED="Loaded",
  ERROR="Error",
  INITIAL="Initial",
}

export enum OperationEnum {
  CREATE = "CREATE",
  UPDATE = "UPDATE",
  ADD = "ADD",
  ERROR = "ERROR"
}